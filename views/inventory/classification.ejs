<%# Display content heading, message and inventory items %>

    <% if (title) { %>
        <h1>
            <%= title %>
        </h1>
        <% } else { res.redirect('/') } %>

            <%# messages %>

                <%- grid %>